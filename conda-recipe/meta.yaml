package:
  name: cadump
  version: 0.0.8

source:
    path: ..

build:
  noarch: python
  entry_points:
    - cadump_server = cadump.cadump:main

requirements:
  build:
    - python
    - data_api
  run:
    - python
    - data_api